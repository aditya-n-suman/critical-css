/**
 * @critical-css/shared — public API barrel (AT-01).
 *
 * Zero dependencies, zero Node.js built-ins: everything here must be safe to
 * reference inside a browser-injected `evaluate()` call.
 */

export * from './dtos/index.js'
export * from './errors/index.js'
